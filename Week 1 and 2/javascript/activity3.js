let totalAmount = prompt('enter your total amount: ')

let tip = prompt('enter your percentage, 0.1, 0.15, 0.2: ')



function tipCalc(totalAmount, tip){
    let result = totalAmount * tip + totalAmount
    return result
}

console.log(result)