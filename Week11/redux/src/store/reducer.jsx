
const initialState = {
    counter: 0
}


// reducer is just a function that takes in onld state and an action and returns the UPDATED STATE
const reducer = (state = initialState, action) => {
    return state 
}

export default reducer